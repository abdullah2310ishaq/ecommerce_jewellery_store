export type Product = {
    id: string; // Change to string if necessary
    name: string;
    price: string;
    img: string;
  };
export type Product = {
  id: string; 
  name: string;
  price: number; // Changed to number for accurate price representation
  img: string;
};  